server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MrGriv/config.git

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka